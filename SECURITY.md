# 安全策略

## 报告漏洞

**不要开公开 issue。** 发到仓库的 Security → "Report a vulnerability"（GitHub
私密报告），或者在仓库 README 里找到维护者邮箱直接发。

请在报告里带上：

- 影响范围（能做什么、需要什么前提）
- 复现步骤（最小可复现最好）
- 受影响的版本（`kp version` 或 `/api/v1/health` 的 `version` 字段）
- 你的判断：这个漏洞严重到什么程度

**48 小时内会得到第一次回复。** 这是个人维护的项目，不是商业产品，但这个
时间承诺是认真的。

## 支持范围

只支持最新版本。这个项目没有 LTS 分支 —— 单二进制、无外部依赖，升级成本
本来就低（`make build` 或重拉镜像）。

## 设计上已知的取舍

开源意味着这些会被人反复问，先写在这里：

**评估这个部署是否适合你之前，请先读这一段。**

### 1. 所有身份都能读全部任务

这是给协作团队用的工具，不是多租户系统。任何持有有效 key 的身份都能读到
这个服务端上的**所有**任务、分段、上报和附件。

- 适合：一个团队、一个组织内部。
- 不适合：拿它给互不信任的多个租户用。

### 2. 权限只有一条规则

`admin` 能做管理动作（建身份、改他人角色、停用、增删角色、删任务、配 webhook），
服务端对每一项都会检查，非 admin 调用一律 403。
其余所有身份读写全部任务。没有更细的权限模型 —— 这是刻意的，为了让它
可解释。

### 3. `POST /api/v1/bootstrap` 在「没有可用 admin」时会重新开放

最后一个 admin 的 key 丢了，系统会锁死：所有管理端点 403，而 API 里没有
任何途径授予 admin。所以 bootstrap 在**系统为空**或**一个启用的 admin 都
没有**时开放，会新建一个拿 admin 的身份。

这不是后门 —— 能调这个端点就等于能访问那台机器，和能直接改数据文件是同
一个信任级别。但这意味着：

> **公网部署必须在入口做鉴权。** 用 Cloudflare Access、内网 VPN、或者
> 干脆只绑 loopback 走 SSH 隧道。见 `docs/deploy-tunnel.md`。

`/api/v1/health` 的 `admins` 字段可以提前发现锁死状态。

### 4. 认证是 Bearer key，没有 session 过期

key 不过期。丢了要显式轮换（`kp identity rotate`，旧 key 立即失效）或停用
（`kp identity disable`）。所以：

- 每个使用者（含每个 agent）一个身份，**不要共用 key**。
- 离开团队的人：先 `disable`，需要时 `rotate`。
- 定期 `kp identity list` 清一遍。

### 5. 默认不加密存储

数据是明文 SQLite + 明文附件文件。磁盘加密靠你的操作系统（FileVault /
LUKS / 云盘加密）。服务端不做应用层加密。

### 6. 附件是内容寻址的

`blobs/<sha256前两位>/<sha256>`。后果：

- 同样的文件传两次只存一份（好处）。
- **删掉一个附件记录不会删文件**（没有引用计数的 GC）。要真正清掉得手工删
  blob。这是已知的、有意留下的简化。

### 7. key 只在创建/轮换时明文出现一次

服务端只存 SHA-256。丢了不能找回，只能轮换。

### 8. 没有速率限制

服务端不限制请求频率。放在公网前面的话，由前面的层（Cloudflare、nginx）
负责。

## 部署安全检查清单

- [ ] 服务绑 `127.0.0.1`，或容器只映射到 loopback；公网走隧道/Access
- [ ] `GET /api/v1/health` 的 `admins` 大于 0（不是锁死状态）
- [ ] 给 agent 发的 key **不带 admin 角色**
- [ ] 每个 agent 一个身份，不共用
- [ ] 磁盘加密开着
- [ ] 定期备份 `keypoint.db` + `blobs/`（见 `docs/deploy-tunnel.md`）
